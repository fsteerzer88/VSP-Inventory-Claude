import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export function LocationPrintPage() {
  return <PlaceholderPage title="Print labels" description="Printable QR label sheet for selected locations." />;
}
