import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export function IntakeWizard() {
  return (
    <PlaceholderPage
      title="Product intake"
      description="Scan a product barcode, capture a label photo, enter details, then scan a location."
    />
  );
}
